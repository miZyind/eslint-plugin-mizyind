import base from './configs/base';
import native from './configs/native';
import next from './configs/next';
import react from './configs/react';
import rules from './rules';

export = { rules, configs: { base, native, next, react } };
